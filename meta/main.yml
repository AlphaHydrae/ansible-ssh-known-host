galaxy_info:
  author: Simon Oulevay (Alpha Hydrae)
  description: Adds hosts to the SSH known_hosts file.
  license: MIT
  min_ansible_version: 2.0

  platforms:
    - name: Darwin
      versions:
        - all
    - name: Fedora
      versions:
        - all
    - name: Ubuntu
      versions:
        - all

  galaxy_tags:
    - ssh
    - host
    - knownhosts

dependencies:
  - role: AlphaHydrae.multipass
